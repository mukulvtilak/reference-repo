import { RedBackgroundDirective } from './red-background.directive';

describe('RedBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new RedBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
